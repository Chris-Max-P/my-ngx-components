import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NightShifterComponent } from './night-shifter.component';
/*

describe('NightShiftComponent', () => {
  let component: NightShifterComponent;
  let fixture: ComponentFixture<NightShifterComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ NightShifterComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(NightShifterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
